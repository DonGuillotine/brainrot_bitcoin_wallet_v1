import 'package:flutter/material.dart';

class LightningSetupScreen extends StatelessWidget {
  const LightningSetupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lightning Setup'),
      ),
      body: const Center(
        child: Text('Lightning Setup Screen - To be implemented'),
      ),
    );
  }
}
